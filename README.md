# Pixel Art Quiz Game (React + Google Sheets)

這是一個復古像素風格的網頁問答遊戲。前端使用 React Vite 開發，後端透過 Google Apps Script (GAS) 連接 Google Sheets 作為資料庫（題目來源與成績記錄）。

## 🎮 快速開始

### 1. 安裝與執行
確保電腦已安裝 Node.js。

```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev
```
啟動後，瀏覽器打開 `http://localhost:5173` 即可遊玩。

---

## 🛠️ Google Sheets & Apps Script 配置教學

要讓遊戲使用真實的題目與記錄成績，你需要配置自己的 Google Sheet。

### 步驟 1：建立 Google Sheet
1. 到 [Google Sheets](https://sheets.new) 建立一個新試算表。
2. **建立兩個工作表（Tabs）**，名稱必須完全一致：
    - `題目`
    - `回答`

### 步驟 2：設定工作表欄位

#### `題目` 工作表
請在第一列（Row 1）設定以下標題：
| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| 題號 | 題目 | A | B | C | D | 解答 |

> 💡 **提示**：你可以直接複製下方的 [測試題目](#-測試題目生成式-ai-基礎知識) 到這張表。

#### `回答` 工作表
請在第一列（Row 1）設定以下標題：
| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| ID | 闖關次數 | 總分 | 最高分 | 第一次通關分數 | 花了幾次通關 | 最近遊玩時間 |

### 步驟 3：安裝 Google Apps Script (GAS)
1. 在你的 Google Sheet 中，點擊上方選單 **「擴充功能」 (Extensions) > 「Apps Script」**。
2. 刪除編輯器中預設的 `myFunction`。
3. 將本專案 `backend_code.js` (或下方附錄代碼) 的內容完整複製貼上到編輯器中。
4. 點擊磁碟片圖示 💾 儲存專案，命名為 `PixelGameAPI`。

#### 附錄：GAS 核心代碼
(若找不到 `backend_code.js`，請使用以下代碼)
```javascript
// 見專案中的 backend_code.js 檔案，或向 AI 索取
```

### 步驟 4：部署為網頁應用程式 (Web App)
1. 在 Apps Script 頁面右上角，點擊 **「部署」 (Deploy) > 「新增部署」 (New deployment)**。
2. 點擊左側齒輪圖示，選擇 **「網頁應用程式」 (Web app)**。
3. 設定如下：
    - **描述**：Pixel Game API
    - **執行身份 (Execute as)**：**我 (Me)** (非常重要！)
    - **誰可以存取 (Who has access)**：**所有人 (Anyone)** (這樣前端才能呼叫)
4. 點擊 **「部署」 (Deploy)**。
5. **授權**：第一次部署會要求授權，請點擊「核對權限」 -> 選擇帳號 -> Advanced (進階) -> Go to PixelGameAPI (unsafe)。
6. 複製生成的 **網頁應用程式網址 (Web App URL)** (以 `/exec` 結尾)。

### 步驟 5：連接前端 (.env)
1. 回到本專案根目錄，找到或建立 `.env` 檔案。
2. 將你的網頁應用程式網址填入 `VITE_GOOGLE_APP_SCRIPT_URL`：

```ini
VITE_GOOGLE_APP_SCRIPT_URL=https://script.google.com/macros/s/你的_SCRIPT_ID/exec
VITE_PASS_THRESHOLD=3
VITE_QUESTION_COUNT=5
```
3. **重啟前端**：修改 `.env` 後必須按 `Ctrl+C` 停止並重新執行 `npm run dev`。

---

## 📝 測試題目：【生成式 AI 基礎知識】

請複製下表內容（此Markdown表格可直接貼到 Google Sheets）：

| 題號 | 題目 | A | B | C | D | 解答 |
|---|---|---|---|---|---|---|
| 1 | ChatGPT 是基於哪種神經網路架構？ | CNN (卷積神經網路) | RNN (遞歸神經網路) | Transformer | GAN (對抗生成網路) | C |
| 2 | 在生成式 AI 中，「幻覺 (Hallucination)」是指什麼？ | AI 生成看起來真實但錯誤的資訊 | AI 拒絕回答問題 | AI 生成速度太慢 | AI 能夠預測未來 | A |
| 3 | 下列哪個不是常見的生成式 AI 模型？ | GPT-4 | Midjourney | Stable Diffusion | Excel | D |
| 4 | 「Prompt Engineering」的主要目的是什麼？ | 優化電腦硬體 | 設計更有效的提示詞來引導 AI | 編寫 Python 程式碼 | 訓練新的基礎模型 | B |
| 5 | LLM 代表什麼縮寫？ | Small Language Model | Large Language Model | Long Learning Machine | Logic Learning Module | B |
| 6 | 在圖像生成 AI 中，通常使用什麼技術將雜訊轉變為清晰圖像？ | 擴散模型 (Diffusion Model) | 線性回歸 | 決策樹 | K-means 分群 | A |
| 7 | 下列哪項是 RAG (檢索增強生成) 的主要優勢？ | 讓模型不需連網 | 減少模型參數量 | 允許模型使用即時或私有的外部知識 | 增加模型的幻覺 | C |
| 8 | 訓練 ChatGPT 這類模型通常需要大量的什麼？ | 標記好的圖片 | 文字數據 (Text Data) | 音頻文件 | 3D 模型 | B |
| 9 | 哪個參數通常用來控制 AI 輸出的隨機性或創造力？ | Batch Size | Learning Rate | Temperature (溫度) | Epoch | C |
| 10 | 「Token」在大型語言模型中通常指的是？ | 區塊鏈代幣 | 模型的付費貨幣 | 文本處理的最小單位 (如字詞片段) | API 金鑰 | C |

---

## 🚀 GitHub Pages 自動部署教學

本專案已設定好 GitHub Actions，可以自動將網站部署到 GitHub Pages。

### 步驟 1：建立 GitHub Repository
1. 在 GitHub 建立一個新的 Repository。
2. 將本專案代碼推送到該 Repository。

### 步驟 2：設定 Secrets (環境變數)
為了讓 GitHub Actions 能正確打包（讀取 .env 中的設定），你需要將 .env 的內容設定到 GitHub Secrets 中。

1. 進入你的 Repository 頁面。
2. 點擊 **Settings > Secrets and variables > Actions**。
3. 點擊 **New repository secret**，依序新增以下 3 個變數 (參考 `.env.example`):
    - `VITE_GOOGLE_APP_SCRIPT_URL`: 填入你的 Google Web App URL
    - `VITE_PASS_THRESHOLD`: 例如 `3`
    - `VITE_QUESTION_COUNT`: 例如 `5`

### 步驟 3：設定 GitHub Pages 來源
1. 點擊 **Settings > Pages**。
2. 在 **Build and deployment > Source** 選擇 **Deploy from a branch** (如果不需調整)。
3. 但因為我們使用自定義 Actions，請確認部署完成後，gh-pages 分支是否建立。
   ***更正**：本專案使用 `JamesIves/github-pages-deploy-action`，它會自動建立並推送 `gh-pages` 分支。
4. 當 Actions 跑完第一次後，回到 **Settings > Pages**。
5. 將 **Source** 改為 `Deploy from a branch`。
6. 將 **Branch** 選為 `gh-pages` / `/ (root)`。
7. 點擊 **Save**。

幾分鐘後，你就可以在 `https://<你的帳號>.github.io/<你的專案名稱>/` 看到遊戲了！

